---
name: ai-task-navigator
description: 根据真实任务拆解步骤、核实当前 Agent 能力，检索适合的 AI 方法、skill、插件和外部工具，交付附信息源的使用路径。用于用户不知道怎样借助 AI 完成某件事、如何选工具或希望尝试新方法时；单纯解释术语或已有明确方案的执行请求不需要完整导航。
---

# AI 任务导航

帮助 AI 使用经验有限的人完成眼前任务，并在使用过程中理解能力、建立选择信心。交付可行动的使用路径和有依据的新方法；不以工具数量或术语数量衡量质量。

## 1. 理解任务与当前阶段

- 先利用用户已提供的材料、相关项目文件和可访问的上下文，识别成果、验收标准、现有进展、真正卡点及时间、预算等约束。只读取与任务有关的资料。
- 用户可能是从零开始，也可能已有大量成果。对后者从最近的未完成环节出发，不默认重做或沿用旧路线；比较新增方案带来的收益与迁移成本。
- 仅追问会改变路线的缺失信息。其余明确列为假设，先给可调整的方案。不要让新手先填写术语、工具和模式清单。
- 区分当前交付与远期设想。例如展示视频和可在线游玩的场景可能需要不同路径，不能因标题相似就扩大范围。

## 2. 核实能力环境

分别确认：

1. **产品支持**：用户使用的 Agent 产品、模式、相关功能；变化中的信息查官方资料，遵循宿主的文档访问规则。
2. **会话可用**：工具列表、已安装 skill/插件、文件与运行环境、权限。原生能力、模式或插件实际开放后才视为可调用。
3. **新增条件**：外部服务、安装、账号、费用，以及用户愿意承担的操作与学习成本。

能从环境识别的先识别；无法确认目标产品且会影响结论时再问。当前助手的能力不等于用户另一个 Agent 的能力。用“当前可用 / 需要配置 / 尚未确认”标记关键依赖，不默认扫描无关配置或凭据。

## 3. 拆解并发现方法

先按成果与难点拆步骤，再围绕难点搜索；任务简单时可以合并环节。

- 用“任务/难点 + 工作流/技巧/skill/工具”等中文及英文关键词寻找候选，结合目标 Agent、文件格式和限制缩小范围。
- 对有价值的新方法进行主动发现，不只核实现有工具：按任务适配度检索 X 原帖、GitHub 项目或 skill、论坛经验、官方教程。无需每次覆盖所有平台。
- 新、热门、高星用于发现线索，不能替代适配性和质量判断。只有实际核实时才报告日期、星数、互动量，并注明检索日期；不把搜索排序称为热度排行。
- 先做有界检索；当主要难点已有有依据的路径，新增结果重复或与任务无关时停止。允许没有值得增加的工具，也允许明确推荐更换路线。
- 无联网能力、平台不可访问或仅检索到摘要时说明覆盖与证据限制，仍交付基于已有信息的暂定路径及待核实项，不声称搜过或验证过。

在评估检索结果和编写推荐前，读取 [来源与推荐规范](references/source-guide.md)。

## 4. 将能力匹配到具体步骤

为每一步明确：做什么、用什么、为什么、如何开始、产出什么、怎样验收。现成能力够用时直接使用；如果专业工具明显提高质量或降低成本，说明引入价值。

| 类别 | 选择依据 |
| --- | --- |
| 思考方法 | 第一性原理用于目标或假设不清；对抗式审查用于能具体指出触发条件和后果的重要风险，不机械加到每一步 |
| 原生能力与模式 | 当前 Agent 已能完成的分析、搜索、编码、图像或文件任务；模式可用性需确认 |
| Skill | 需要稳定复用的专业流程、格式与验收规则；阅读具体内容和依赖后再评估，标题不能证明效果 |
| MCP、插件 | 需要连接外部数据或应用；区分连接方式与工作流程，插件可能同时包含 skill 和工具 |
| CLI、脚本 | 需要批量、精确、可重复操作，且运行环境可用；可由 Agent 操作，不默认要求用户学命令 |
| 外部 AI 服务 | 当前环境缺少关键生成或专业处理能力；评估输出能否接入下一步及额外成本 |
| Loop | 有明确的执行、检查、修正与停止条件；无进展则诊断根因或换方法，不无限重试 |

这些类别可以组合，不必为了覆盖概念而各推荐一个。方法若只来自本次推理，标为“本次分析建议”，不要虚构权威来源。

对重要路线做一轮有边界审查：指出少量会实质影响目标或验收的问题，并给最低成本验证动作。复杂、收费或依赖很多的新路线，优先提出一个能走通输入到验收的小样本实验。用户给出新事实后才有必要再审查。

## 5. 输出导航

默认用中文、解释必要术语。围绕用户任务写，不先上概念课，不默认交付长篇提示词。篇幅随任务缩放；简单任务可用几个短段落，不必铺满所有标题。

包含以下信息，可自然合并：

1. **任务判断**：成果、当前卡点及会改变路线的假设。
2. **推荐主路径**：逐步说明行动、方法/工具、作用与验收；关键依赖标记可用状态。
3. **对应信息源**：在每个外部推荐旁放具体链接、条件和验证状态，区分来源事实与本次判断。
4. **可选尝试**：有价值时给少量候选，说明改善什么、额外投入、最小试验和保留条件；没有合适候选就直说。
5. **现在做什么**：给一个具体起点；概括本次搜索范围与尚未确认的信息。

推荐复杂项目时可以用步骤表，关键推荐展开为短卡片。不要把不确定的成本填成零，不把社区案例写成效果保证，不用“你已经落后”等表述制造焦虑。

默认交付导航与试验建议。安装、注册、付费、上传用户材料或推进原任务，依用户已有授权与实际请求处理；导航请求本身不意味着已授权这些操作。
